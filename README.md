# BharatRouter Homebrew tap

```sh
brew install bharatrouter/tap/brcode
```

`brcode` is a Codex-/Claude-Code-style coding agent on GLM through
[BharatRouter](https://bharatrouter.com/code). Then: `brcode login && brcode init && brcode`.
